# Spacing

Use spacing tokens from `@design-studio/tokens` or CSS variables.

Examples:

- `var(--space-4)`
- `var(--space-8)`
- `var(--space-16)`

Spacing scale is shared across web and native surfaces for parity.

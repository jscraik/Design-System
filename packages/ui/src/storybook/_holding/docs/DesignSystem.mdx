import { Meta, Canvas, Story, ColorPalette, ColorItem } from "@storybook/addon-docs/blocks";
import { colors } from "../../utils/theme";
import * as DesignSystemStories from "./DesignSystem.stories";

<Meta of={DesignSystemStories} />

# Design System

The aStudio design system provides a comprehensive set of design tokens, components, and guidelines to ensure consistency across all applications.

## 🎨 Color System

Our color system is built around semantic color tokens that adapt to different themes and contexts.

### Core Theme Colors

<ColorPalette>
  <ColorItem
    title="Background"
    subtitle="Primary app canvas and shell surfaces"
    colors={{ background: "var(--background)" }}
  />
  <ColorItem
    title="Secondary"
    subtitle="Muted and secondary surfaces"
    colors={{ secondary: "var(--secondary)" }}
  />
  <ColorItem
    title="Foreground"
    subtitle="Primary text and icon tone"
    colors={{ foreground: "var(--foreground)" }}
  />
  <ColorItem
    title="Muted Foreground"
    subtitle="Supporting copy and metadata"
    colors={{ "muted-foreground": "var(--muted-foreground)" }}
  />
</ColorPalette>

### Accent Colors

<ColorPalette>
  <ColorItem
    title="Gray"
    subtitle="Neutral and secondary elements"
    colors={{ gray: "var(--accent-gray)" }}
  />
  <ColorItem
    title="Red"
    subtitle="Errors and destructive actions"
    colors={{ red: "var(--accent-red)" }}
  />
  <ColorItem
    title="Orange"
    subtitle="Warnings and attention"
    colors={{ orange: "var(--accent-orange)" }}
  />
  <ColorItem
    title="Yellow"
    subtitle="Caution and highlights"
    colors={{ yellow: "var(--accent-yellow)" }}
  />
  <ColorItem
    title="Green"
    subtitle="Success and positive actions"
    colors={{ green: "var(--accent-green)" }}
  />
  <ColorItem
    title="Blue"
    subtitle="Primary accent color"
    colors={{ blue: "var(--accent-blue)" }}
  />
  <ColorItem
    title="Purple"
    subtitle="Special features and premium"
    colors={{ purple: "var(--accent-purple)" }}
  />
  <ColorItem
    title="Pink"
    subtitle="Creative and expressive elements"
    colors={{ pink: "var(--accent-pink)" }}
  />
</ColorPalette>

## 📏 Spacing System

Our spacing system uses a consistent scale based on rem units:

<Canvas>
  <Story name="Spacing Scale">
    <div className="p-6 space-y-4">
      <div className="space-y-3">
        <div className="flex items-center gap-4">
          <div className="w-20 text-sm text-foreground">
            xs (0.25rem)
          </div>
          <div className="h-4 bg-accent-blue" style={{ width: "0.25rem" }} />
        </div>
        <div className="flex items-center gap-4">
          <div className="w-20 text-sm text-foreground">
            sm (0.5rem)
          </div>
          <div className="h-4 bg-accent-blue" style={{ width: "0.5rem" }} />
        </div>
        <div className="flex items-center gap-4">
          <div className="w-20 text-sm text-foreground">
            md (1rem)
          </div>
          <div className="h-4 bg-accent-blue" style={{ width: "1rem" }} />
        </div>
        <div className="flex items-center gap-4">
          <div className="w-20 text-sm text-foreground">
            lg (1.5rem)
          </div>
          <div className="h-4 bg-accent-blue" style={{ width: "1.5rem" }} />
        </div>
        <div className="flex items-center gap-4">
          <div className="w-20 text-sm text-foreground">
            xl (2rem)
          </div>
          <div className="h-4 bg-accent-blue" style={{ width: "2rem" }} />
        </div>
        <div className="flex items-center gap-4">
          <div className="w-20 text-sm text-foreground">
            2xl (3rem)
          </div>
          <div className="h-4 bg-accent-blue" style={{ width: "3rem" }} />
        </div>
      </div>
    </div>
  </Story>
</Canvas>

## 🔤 Typography

Typography follows a clear hierarchy with consistent sizing and spacing:

<Canvas>
  <Story name="Typography Scale">
    <div className="p-6 space-y-4">
      <h1 className="text-3xl font-bold text-foreground">
        Heading 1 - 3xl/bold
      </h1>
      <h2 className="text-2xl font-semibold text-foreground">
        Heading 2 - 2xl/semibold
      </h2>
      <h3 className="text-xl font-semibold text-foreground">
        Heading 3 - xl/semibold
      </h3>
      <h4 className="text-lg font-medium text-foreground">
        Heading 4 - lg/medium
      </h4>
      <p className="text-base text-foreground">
        Body text - base/normal
      </p>
      <p className="text-sm text-muted-foreground">
        Small text - sm/normal
      </p>
      <p className="text-xs text-muted-foreground">
        Caption text - xs/normal
      </p>
    </div>
  </Story>
</Canvas>

## 🎯 Component Variants

### Size Variants

Most components support consistent size variants:

- `sm` - Small, compact size
- `md` - Medium, default size
- `lg` - Large, prominent size

### Visual Variants

Common visual variants across components:

- `ghost` - Transparent background, visible on hover
- `outline` - Border with transparent background
- `solid` - Filled background

### State Variants

Interactive components support these states:

- `default` - Normal, interactive state
- `active` - Currently selected or pressed
- `disabled` - Non-interactive state
- `loading` - Processing state

## ♿ Accessibility Guidelines

### Color Contrast

All color combinations meet WCAG AA standards:

- Text on backgrounds: minimum 4.5:1 ratio
- Large text: minimum 3:1 ratio
- Interactive elements: minimum 3:1 ratio

### Focus Management

- All interactive elements are keyboard accessible
- Focus indicators are clearly visible
- Focus order follows logical tab sequence

### Screen Reader Support

- Semantic HTML elements used where appropriate
- ARIA attributes provided for complex interactions
- Alternative text for icons and images

## 🎨 Usage Examples

### CSS Custom Properties

```css
.my-component {
  background-color: var(--background);
  color: var(--foreground);
  border-color: var(--accent-blue);
  padding: var(--spacing-md);
  border-radius: var(--radius-lg);
}
```

### Tailwind Classes

```tsx
<div className="bg-background text-foreground p-4 rounded-lg border border-border">
  Content with design tokens
</div>
```

### Theme Utilities

```tsx
import { colors, spacing } from "@design-studio/ui";

const styles = {
  backgroundColor: "var(--background)",
  padding: spacing.md,
};
```

## 🔧 Customization

### Override CSS Variables

```css
:root {
  --accent-blue: var(--accent-purple); /* Retint the accent */
  --background: var(--card); /* Reuse the elevated surface tone */
}
```

### Component-Specific Overrides

```tsx
<Button
  className="bg-accent-purple text-accent-foreground hover:bg-accent-purple/90"
  style={{ "--button-radius": "0.75rem" }}
>
  Custom Button
</Button>
```

The design system ensures consistency while providing flexibility for customization and theming.
